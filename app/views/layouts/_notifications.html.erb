<% if current_user %>
  <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
      <span class="badge rounded-pill bg-danger">
        <%= @unread.count >= 9 ? "9+" : @unread.count %> <i class="bi bi-suit-heart-fill"></i>
      </span>
    </a>
    <ul class="dropdown-menu dropdown-menu-lg-end" aria-labelledby="navbarDropdown">
      <% @unread.each do |nt| %>
        <li class="dropdown-item">
          <%= link_to nt.to_notification.message, nt.to_notification.url %>
          <%#= link_to "Words of the link",  %>
        </li>
      <% end %>
      <% if @read.count > 0 && @unread.count > 0 %>
        <li>
          <hr class="dropdown-divider">
        </li>
      <% elsif @read.count + @unread.count == 0 %>
        <li>
          No notifications yet.
        </li>
      <% end  %>
      <% @read.each do |nt| %>
        <li class="dropdown-item">
          <%= link_to nt.to_notification.message, nt.to_notification.url %>
          <%#= link_to "Words of the link",  %>
        </li>
      <% end %>
    </ul>
  </li>
<% end %>